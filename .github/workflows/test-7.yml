name: a test 7

on:
  workflow_dispatch:

jobs:
  build-1:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        exclude_dir: ["", "ninja/src/third_party/*"]

    name: Libtorrent ${{ matrix.patch_test }} ${{ matrix.with_cmake_env }}

    steps:
      - uses: actions/checkout@v4
      - uses: codespell-project/actions-codespell@v2
        with:
          skip: ${{ matrix.exclude_dir }}
          ignore_words_list: fo,wee,addin,notin
